server:
  port: 8888
spring:
  profiles:
    active: default

  application:
      name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Naresh-Kushwaha/Quiz-MicroService-Config

#        native:
#          search-locations: classpath:/configurations

